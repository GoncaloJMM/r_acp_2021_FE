@model IEnumerable<D07_EF6_MVC_DF_02.Models.SubCategoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SubCategoria1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categories.CategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubCategoria1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categories.CategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubCategoriaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubCategoriaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubCategoriaID })
        </td>
    </tr>
}

</table>
